
## 第一套

```mysql
1.insert into tb_dept values ("D4","公关部","Liming","010-82953306");
2.alter table tb_employee modeify salary decimal default 3500;
3.select count(1) AS "总人数" from tb_employee where deptno = (select deptno where dname = "销售部");
4.create view v_emp(eno,ename,age,salary) as select eno,ename,age,salary from tb_employee where deptno = (select deptno where dname = "采购部");
5.不会
```

## 第二套

```mysql
1.select sum(price) as 'total' from tb_commodity where cname = '电视机' and origin = '北京';
2.alter table tb_commodity drop desc1;
3.insert into tb_commodity(cname,ctype,origin,birth,price) values('钢笔','文具','上海','2021-12-25',25);
4.create view v_bjcommodity as select * from tb_commodity where origin = '北京';
5.create user 'client'@'localhost' # 不会
```

## 第三套

```mysql
1.alter table tb_student add ssex char(1) default 'M';
2.update tb_student set smajor = '计算机' where sno = '100';
3.create view v_avg(canme,caverage) as select cname avg(grade) from tb_score group by cname;
4.不会
5.create user 'newuser'@'localhost';
grant select on db_student.tb_student to 'newuser'@'localhost';
```


## 第四套
```mysql
1.create table Dept1(
	deptno int,
	dname char(20),
	higherdeptno int default NULL,
	主外键关联不会
);
2.insert into Dept2 valuse(9,'newdept',NULL);
3.create view SJAVA as S.sno,S.sname,SC.grade from S,C,SC where SC.sno = S.sno and SC.cno = C.cno and C.cname = 'JAVA';
4.update SC set grade = 90 where cno = (select cno from C where cname = 'DB') and sno = (select sno from S where sname = '李红');
5.create user 'backupdb'@'localhost' identified by 'backup'; 
```

## 第五套

```mysql

```